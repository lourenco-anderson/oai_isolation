/* log.h – compatibilidade standalone (substitui OAI logger) */
#pragma once
#define LOG_D(channel, fmt, ...)  do {} while(0)
#define LOG_I(channel, fmt, ...)  do {} while(0)
#define LOG_W(channel, fmt, ...)  do {} while(0)
#define LOG_E(channel, fmt, ...)  do {} while(0)
#define PHY  0
