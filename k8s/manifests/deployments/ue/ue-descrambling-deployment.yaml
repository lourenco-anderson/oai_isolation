apiVersion: apps/v1
kind: Deployment
metadata:
  name: ue-descrambling
  namespace: oai-isolation
  labels:
    app: ue-descrambling
    component: ue
    function: descrambling
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: ue-descrambling
  template:
    metadata:
      labels:
        app: ue-descrambling
        component: ue
        function: descrambling
    spec:
      containers:
      - name: ue-descrambling
        image: oai-isolation:ue-descrambling
        imagePullPolicy: IfNotPresent
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
        env:
        - name: LOG_LEVEL
          value: "INFO"
        securityContext:
          runAsNonRoot: false
          allowPrivilegeEscalation: false
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: ue-descrambling
  namespace: oai-isolation
  labels:
    app: ue-descrambling
spec:
  type: ClusterIP
  ports:
  - protocol: TCP
    port: 9083
    targetPort: 8080
    name: http
  selector:
    app: ue-descrambling
