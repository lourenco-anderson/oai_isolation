apiVersion: apps/v1
kind: Deployment
metadata:
  name: nr_descrambling
  namespace: oai-functions
  labels:
    app: nr_descrambling
    component: oai-function
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nr_descrambling
  template:
    metadata:
      labels:
        app: nr_descrambling
        component: oai-function
    spec:
      containers:
      - name: nr_descrambling
        image: ${DOCKER_REGISTRY:-docker.io/your-username}/nr_descrambling:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8010
          name: http
          protocol: TCP
        env:
        - name: PORT
          value: "8010"
        - name: OAI_LIB_PATH
          value: "/usr/local/lib/liboai_functions.so"
        resources:
          requests:
            memory: "256Mi"
            cpu: "200m"
          limits:
            memory: "512Mi"
            cpu: "400m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8010
          initialDelaySeconds: 15
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /ready
            port: 8010
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
      restartPolicy: Always
