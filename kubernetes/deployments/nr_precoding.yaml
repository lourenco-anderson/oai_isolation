apiVersion: apps/v1
kind: Deployment
metadata:
  name: nr_precoding
  namespace: oai-functions
  labels:
    app: nr_precoding
    component: oai-function
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nr_precoding
  template:
    metadata:
      labels:
        app: nr_precoding
        component: oai-function
    spec:
      containers:
      - name: nr_precoding
        image: ${DOCKER_REGISTRY:-docker.io/your-username}/nr_precoding:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8004
          name: http
          protocol: TCP
        env:
        - name: PORT
          value: "8004"
        - name: OAI_LIB_PATH
          value: "/usr/local/lib/liboai_functions.so"
        resources:
          requests:
            memory: "192Mi"
            cpu: "150m"
          limits:
            memory: "384Mi"
            cpu: "300m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8004
          initialDelaySeconds: 15
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /ready
            port: 8004
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
      restartPolicy: Always
