apiVersion: apps/v1
kind: Deployment
metadata:
  name: nr_ofdm_demo
  namespace: oai-functions
  labels:
    app: nr_ofdm_demo
    component: oai-function
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nr_ofdm_demo
  template:
    metadata:
      labels:
        app: nr_ofdm_demo
        component: oai-function
    spec:
      containers:
      - name: nr_ofdm_demo
        image: ${DOCKER_REGISTRY:-docker.io/your-username}/nr_ofdm_demo:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8008
          name: http
          protocol: TCP
        env:
        - name: PORT
          value: "8008"
        - name: OAI_LIB_PATH
          value: "/usr/local/lib/liboai_functions.so"
        resources:
          requests:
            memory: "192Mi"
            cpu: "150m"
          limits:
            memory: "384Mi"
            cpu: "300m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8008
          initialDelaySeconds: 15
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /ready
            port: 8008
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
      restartPolicy: Always
