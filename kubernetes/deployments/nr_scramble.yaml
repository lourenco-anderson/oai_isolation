apiVersion: apps/v1
kind: Deployment
metadata:
  name: nr_scramble
  namespace: oai-functions
  labels:
    app: nr_scramble
    component: oai-function
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nr_scramble
  template:
    metadata:
      labels:
        app: nr_scramble
        component: oai-function
    spec:
      containers:
      - name: nr_scramble
        image: ${DOCKER_REGISTRY:-docker.io/your-username}/nr_scramble:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8001
          name: http
          protocol: TCP
        env:
        - name: PORT
          value: "8001"
        - name: OAI_LIB_PATH
          value: "/usr/local/lib/liboai_functions.so"
        resources:
          requests:
            memory: "256Mi"
            cpu: "200m"
          limits:
            memory: "512Mi"
            cpu: "400m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8001
          initialDelaySeconds: 15
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /ready
            port: 8001
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
      restartPolicy: Always
